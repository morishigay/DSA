#include <iostream>
#include <stdlib.h>

class Node{
    public:
        int data;
        Node* left;
        Node* right;

        Node* newNode(int x){
            data = x;
            left = right = nullptr;
        }
};

int maxValue(Node* root){

}

Node* insert(Node* root, int x){
    if (root == NULL){
        
    }
}

int main(){
    return 0;
}